// VALID: sizeof expression returns int and is a constant
typ record = (x: int, y: char)

fun main() : int =
    let
        var sz : int
        var sz2 : int
    in
        sz = sizeof(record),
        sz2 = sizeof(int),
        sz + sz2
    end
